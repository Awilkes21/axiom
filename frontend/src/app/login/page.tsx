import { PageShell } from "@/components/layout/page-shell";

export default function LoginPage() {
  return (
    <PageShell title="Login">
      <p className="text-slate-600">
        Login form placeholder. Unauthorized API responses can redirect here.
      </p>
    </PageShell>
  );
}
